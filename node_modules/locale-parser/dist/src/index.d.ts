/// <reference types="locale-parser" />
import { I18nOptions, I18nToJSON, I18nConstants, I18nArgs } from "parsertypes";
export declare class I18n {
    private locales;
    private directory;
    private defaultLocale;
    private constants?;
    constructor(options: I18nOptions);
    private resolveString;
    private replace;
    getLocales(): string[];
    getConstant(constant?: string): string | I18nConstants | undefined;
    toJSON(args?: I18nArgs): I18nToJSON;
    get(locale: string, section: string, key: string, args?: I18nArgs): string | string[];
}
